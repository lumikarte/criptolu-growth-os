# Dependencias de desarrollo/test (no van a producción).
-r requirements.txt
pytest>=8.0
httpx>=0.27          # TestClient de Starlette/FastAPI
fakeredis>=2         # CRI-603: tests del encolado real sin un Redis corriendo
